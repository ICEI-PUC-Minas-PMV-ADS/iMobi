import { create } from "./create"
import { getAll } from "./getAll"
import { getByUserId } from "./getByUserId"
import { getByCidade } from "./getByCidade"

export const imovelService = {
  create,
  getAll,
  getByUserId,
  getByCidade
}
