{
  "dependencies": {
    "@prisma/client": "^5.2.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "prisma": "^5.2.0",
    "tsx": "^3.12.7",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "docker build -t ms_broker .",
    "build:docker": "tsc --build",
    "migrate:docker": "npx prisma migrate dev --name first-migration",
    "start:docker": "npm run migrate:docker && tsx src/server.ts",
    "start": "docker compose up"
  }
}
