import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  heading: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#333', // Cor escura para melhor legibilidade
    marginBottom: 10,
  },
  subheading: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#555', // Cor um pouco mais clara
    marginBottom: 8,
  },
  paragraph: {
    fontSize: 16,
    color: '#666', // Cor ainda mais clara
    marginBottom: 16,
  },
});

export default styles;
